<h1>New visit</h1>

<%= form_tag patient_visits_path, :method => :post, :class => "form-horizontal" do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="span6">
    <div class="field">
      <%= select_tag "typelist", "<option value=''>Please select an option</option><option value='1'>Consultation</option><option value='2'>Progress Note</option><option value='3'>Injection Session</option>".html_safe %>
    </div>
    <div class="field">
      <%= select_tag("patients", options_for_select(@patients)) %>
    </div>
    <div class="field">
      <%= select_tag("staffs", options_for_select(@staffs)) %>
    </div>

    <div class="form-actions">
      <button type="submit" class="btn btn-primary">Create</button>
      <%= link_to 'Back', patient_visits_path, :class => "btn" %>
    </div>
  </div>
<% end %>
