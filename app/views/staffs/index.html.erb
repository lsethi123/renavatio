<% cols = Staff.columns_hash.keys.map{|p| p.to_s.camelcase } %>
<% display_columns = Staff.columns_hash.keys.map{|p| p } %>
<% cols = cols.map{|c| c.split.join(" ")} %>
<% staff = Staff.new() %>

<%= render :partial => "shared/collections",
  :locals => {
    :collection => create_collections(@staffs, "generate_new_staff"),
    :columns => ["Id", "Name", "Email", "Title", "Administrator", "CreatedAt", "UpdatedAt"],
    :options => "create_staff_options"
  }
%>